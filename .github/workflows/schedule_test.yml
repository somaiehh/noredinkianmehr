name: Schedule Test

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
  schedule:
    - cron: '6,21,36,51 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled test
        run: echo "SCHEDULE WORKS"
