name: Schedule Test

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
  schedule:
    - cron: '11,26,41,56 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled test
        run: echo "SCHEDULE WORKS"
