name: Schedule Test

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
  schedule:
    - cron: '2,17,32,47 * * * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled test
        run: echo "SCHEDULE WORKS"
